services:
  app:
    build: .
    ports:
      - "5173:80"
    volumes:
      - ./backend/tmp:/app/tmp
    env_file:
      - ./backend/.env
    environment:
      - TMP_DIR=/app/tmp
    restart: unless-stopped
